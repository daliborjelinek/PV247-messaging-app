import * as React from 'react';

export class MessageEditor extends React.PureComponent {

  public render(): JSX.Element {
    return (
      <div className={'MessageEditor'}> Here is going to be WYSIWYG editor.</div>
    );
  }
}
